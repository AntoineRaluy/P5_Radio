{% extends 'base.html.twig' %}

{% block title %}Hello ArticleController!{% endblock %}

{% block body %}
    <p class="d-inline">{{ articleText|raw }}</p>
    <p class="d-inline">{{ acontent|markdown }}</p>
    {% foreach articles as article %}
        {{ article.title }}
        {{ article.content }}
        {{ article.postDate }}
    {% endforeach %}

    
    {% endforeach %}
{% endblock %}
